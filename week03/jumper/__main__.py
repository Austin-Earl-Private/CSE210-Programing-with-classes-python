from week03.jumper.game.director import Director
from week03.jumper.game.parachute import Parachute
if __name__ == "__main__":
    director = Director()
    director.start_game()
    # para = Parachute()
    # para.show_parachute()
    # para.remove_health(1)
    # para.show_parachute()
    # para.remove_health(1)
    # para.show_parachute()
    # para.remove_health(1)
    # para.show_parachute()
    # para.remove_health(1)
    # para.show_parachute()